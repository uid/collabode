<% var memberCount = members.length; %>
<h2><%= memberCount %> item<%= memberCount == 1 ? '' : 's' %></h2>
<table id="filegrid">
<tr>
<% for (var idx = 0; idx < memberCount; idx++) { %>
  <% if (idx > 0 && idx % 5 == 0) { %></tr><tr><% } %>
  <%= helpers.renderView("filegrid", members[idx]) %>
<% } %>
</tr>
</table>
